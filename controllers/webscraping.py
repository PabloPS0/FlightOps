from selenium import webdriver
from selenium.webdriver.common.by import By

# Launch the browser (Chrome or Firefox)
driver = webdriver.Chrome() 
driver.get("https://tycoon.airlines-manager.com") # Navigate to the URL